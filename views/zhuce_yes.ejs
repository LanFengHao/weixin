<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <style media="screen">
      body{
        background-color: #65c294;
      }
    </style>
  </head>
  <body>
    <script src="http://cdn.bootcss.com/socket.io/2.0.3/socket.io.js"></script>
    <script type="text/javascript">
    var socket = io();
    console.log(location.pathname);
    if (location.pathname == '/zhuce') {
      window.location = '/'
    }


    if (location.pathname == '/' || location.pathname == '/ok') {
      var name = document.getElementById('name');
      var paw = document.getElementById('paw');
      socket.emit('name hu', ins.value , paw.value);
      // console.log(ins.value);
      // console.log(paw.value);
                  // socket.on('name hu',function (name,paw) {
                  //   console.log(name);
                  //   console.log(paw);
                  // })
        window.location = '/io'
      // }
    }
    if (location.pathname == '/mima') {
      window.location = '/'
    }
    </script>
  </body>
</html>
